" Set compatibility to Vim only
set nocompatible

" Text wrap that extends beyond the screen length
set wrap

" Files encoding
set encoding=utf-8

" Line numbers
set number

" Relative lines numbers to the current line
set relativenumber

" Status bar
set laststatus=2

" Intent width
set shiftwidth=2
